# Test Cases — AppPageLayoutComponent

| ID del Test                   | Tipo       | Escenario / Propósito                                                                       | Precondiciones                                             | Pasos clave                                                                                             | Resultado Esperado                                          | Justificación de Valor                                                              |
|-------------------------------|------------|---------------------------------------------------------------------------------------------|------------------------------------------------------------|---------------------------------------------------------------------------------------------------------|-------------------------------------------------------------|-------------------------------------------------------------------------------------|
| TC-ResolvedConfigLayoutConfig | Componente | `resolvedConfig` usa `layoutConfig` como máxima prioridad                                   | `layoutConfig`, `preset` y `gridConfig` todos configurados | 1. Proveer los tres inputs. 2. Leer `resolvedConfig()`.                                                 | El config retornado es el `layoutConfig` provisto.          | Garantiza el orden de prioridad de la configuración: `layoutConfig` siempre gana.   |
| TC-ResolvedConfigPreset       | Componente | `resolvedConfig` usa el preset cuando no hay `layoutConfig`                                 | `preset = 'twoColumn'`, sin `layoutConfig`                 | 1. Proveer solo `preset`. 2. Leer `resolvedConfig()`.                                                   | El config coincide con `LAYOUT_PRESETS.twoColumn`.          | Habilita el uso de layouts predefinidos para los casos de uso estándar.             |
| TC-ResolvedConfigGridCells    | Componente | `resolvedConfig` usa `gridConfig` + `cells` cuando no hay `layoutConfig` ni `preset`        | Solo `gridConfig` y `cells` provistos                      | 1. Proveer `gridConfig` y `cells`. 2. Leer `resolvedConfig()`.                                          | El config contiene el grid y las celdas provistas.          | Habilita layouts totalmente customizados para casos de uso únicos.                  |
| TC-ResolvedConfigDefault      | Componente | `resolvedConfig` cae en `fullWidth` cuando no se provee ninguna configuración               | Sin ningún input de layout                                 | 1. Instanciar sin inputs de layout. 2. Leer `resolvedConfig()`.                                         | El config es `LAYOUT_PRESETS.fullWidth`.                    | Garantiza un layout funcional por defecto que no rompe la página.                   |
| TC-SlotContentProjection      | Componente | Proyecta el contenido correcto en el slot correspondiente por `slotId`                      | Preset `twoColumn` (slots `left` y `right`)                | 1. Usar el preset `twoColumn`. 2. Proveer content para los slots `left` y `right`. 3. Detectar cambios. | Cada celda contiene el template de su slot correspondiente. | Happy path crítico: la funcionalidad core de proyección de contenido por slot.      |
| TC-ShowEmptySlots             | Componente | Renderiza el placeholder del slot cuando `showEmptySlots = true` y el slot no tiene content | Slot sin contenido proyectado                              | 1. Setear `showEmptySlots = true`. 2. Detectar cambios.                                                 | La celda muestra el elemento `.empty-slot` con el `slotId`. | Facilita el desarrollo y prototipado al hacer visible el layout sin contenido real. |
| TC-HideEmptySlots             | Componente | No renderiza placeholder cuando `showEmptySlots = false` y el slot está vacío               | Slot sin contenido proyectado                              | 1. Setear `showEmptySlots = false` (default). 2. Detectar cambios.                                      | La celda vacía no contiene ningún elemento visible.         | Garantiza que slots sin contenido no generan elementos DOM fantasma en producción.  |
