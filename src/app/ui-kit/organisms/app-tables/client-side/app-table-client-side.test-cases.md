# Test Cases — AppTableClientSideComponent

| ID del Test                | Tipo       | Escenario / Propósito                                                          | Precondiciones                                                   | Pasos clave                                                                   | Resultado Esperado                                                | Justificación de Valor                                                                         |
|----------------------------|------------|--------------------------------------------------------------------------------|------------------------------------------------------------------|-------------------------------------------------------------------------------|-------------------------------------------------------------------|------------------------------------------------------------------------------------------------|
| TC-DisplayDataNoPagination | Componente | `displayData` retorna todos los datos cuando `showPagination` es `false`       | `config.showPagination = false`, 15 items                        | 1. Configurar sin paginación. 2. Proveer 15 items. 3. Leer `displayData()`.   | Retorna los 15 items sin cortar.                                  | Habilita el modo de tabla sin paginación para conjuntos de datos pequeños.                     |
| TC-DisplayDataPaginated    | Componente | `displayData` retorna solo el slice de la página actual                        | `config.showPagination = true`, `pageSize = 10`, `pageIndex = 1` | 1. Proveer 25 items. 2. Configurar página 2. 3. Leer `displayData()`.         | Retorna los items 11 al 20.                                       | Garantiza que la paginación cliente muestra solo los datos de la página correcta.              |
| TC-FilteringDefault        | Componente | Filtra los datos usando `evaluateCriteria` cuando no se provee `filterFn`      | Datos con campo `name`                                           | 1. Agregar criterio de filtro para `name`. 2. Leer `filteredData()`.          | Solo los items que cumplen el criterio están en `filteredData`.   | Happy path del filtrado cliente: flujo crítico para tablas sin backend.                        |
| TC-FilteringCustomFn       | Componente | Usa `filterFn` personalizada cuando se provee                                  | Input `filterFn` configurado                                     | 1. Proveer `filterFn` custom. 2. Aplicar criterios. 3. Leer `filteredData()`. | Los datos se filtran según la lógica de la función personalizada. | Permite casos de filtrado complejos que la función genérica no puede manejar.                  |
| TC-FiltersChangeResetsPage | Componente | `onFiltersChange` resetea `pageIndex` a 0 cuando `resetPageOnFilter` es `true` | `pageIndex = 2`, `resetPageOnFilter = true`                      | 1. Llamar `onFiltersChange(criteria)`.                                        | `pageIndex` queda en 0 y `filtersChange` emite.                   | Evita que el usuario quede en una página que ya no existe tras aplicar filtros.                |
| TC-FiltersChangeNoReset    | Componente | `onFiltersChange` no resetea `pageIndex` cuando `resetPageOnFilter` es `false` | `pageIndex = 2`, `resetPageOnFilter = false`                     | 1. Llamar `onFiltersChange(criteria)`.                                        | `pageIndex` se mantiene en 2.                                     | Permite mantener la posición de página cuando el filtrado es implícito (ej. búsqueda en vivo). |
| TC-TotalItemCountFiltered  | Componente | `totalItemCount` refleja el número de items filtrados, no el total raw         | 20 items, filtro que retiene 8                                   | 1. Aplicar filtro. 2. Leer `totalItemCount`.                                  | Es 8, no 20.                                                      | La paginación debe basarse en los items filtrados para ser correcta.                           |
