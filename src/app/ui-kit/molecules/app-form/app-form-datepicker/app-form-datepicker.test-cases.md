# Test Cases — AppFormDatepickerComponent

| ID del Test               | Tipo       | Escenario / Propósito                                                   | Precondiciones                      | Pasos clave                                                                                | Resultado Esperado                                                            | Justificación de Valor                                                      |
|---------------------------|------------|-------------------------------------------------------------------------|-------------------------------------|--------------------------------------------------------------------------------------------|-------------------------------------------------------------------------------|-----------------------------------------------------------------------------|
| TC-InitialValue           | Componente | Renderiza el valor inicial del `FormControl` en el input del datepicker | —                                   | 1. Crear `FormControl(new Date('2024-01-15'))`. 2. Setear como input. 3. Detectar cambios. | El input muestra la fecha formateada correctamente.                           | Garantiza la hidratación inicial desde el modelo de datos.                  |
| TC-FormControlSync        | Componente | Actualiza el `FormControl` cuando se selecciona una fecha               | —                                   | 1. Crear `FormControl(null)`. 2. Simular selección de fecha.                               | `control.value` es la fecha seleccionada.                                     | Happy path crítico: sin esto el datepicker no registra la selección.        |
| TC-ErrorDisplayTouched    | Componente | Muestra el error cuando el control es inválido y ha sido tocado         | `Validators.required`               | 1. Marcar el control como tocado. 2. Detectar cambios.                                     | Existe `mat-error` con el mensaje de error.                                   | Garantiza el feedback de validación en el flujo estándar.                   |
| TC-ErrorHiddenUntouched   | Componente | No muestra error cuando el control es inválido pero no tocado           | `Validators.required`               | 1. No tocar el control. 2. Detectar cambios.                                               | No existe `mat-error`.                                                        | Evita errores prematuros antes de la interacción del usuario.               |
| TC-CustomErrorMessage     | Componente | `config.errorMessages` sobreescribe el mensaje de error por defecto     | Control tocado e inválido           | 1. Setear `config.errorMessages` con mensaje custom. 2. Detectar cambios.                  | `mat-error` muestra el mensaje personalizado.                                 | Permite mensajes contextuales por campo.                                    |
| TC-DisabledState          | Componente | Deshabilita el input cuando el `FormControl` está deshabilitado         | —                                   | 1. Crear `FormControl({ value: null, disabled: true })`. 2. Detectar cambios.              | El input del datepicker está deshabilitado.                                   | Previene selección de fechas en campos de solo lectura.                     |
| TC-IsRequired             | Componente | `isRequired()` es `true` y el input tiene atributo `required`           | `Validators.required`               | 1. Crear `FormControl(null, Validators.required)`. 2. Detectar cambios.                    | `component.isRequired() === true`.                                            | Garantiza accesibilidad y semántica HTML correcta.                          |
| TC-MaxDateValidation      | Componente | Muestra error cuando la fecha supera `maxDate`                          | `config.maxDate` configurado        | 1. Setear `config.maxDate`. 2. Ingresar fecha posterior. 3. Marcar como tocado.            | Aparece el error `matDatepickerMax`.                                          | Edge case crítico de negocio: rangos de fechas inválidos deben bloquearse.  |
| TC-MinMaxDateConstraints  | Componente | Aplica `minDate` y `maxDate` al datepicker                              | `config.minDate` y `config.maxDate` | 1. Setear ambas fechas límite. 2. Detectar cambios.                                        | Los atributos `min` y `max` del datepicker reflejan los valores configurados. | Garantiza que restricciones de negocio (ej. rango de reservas) se apliquen. |
| TC-DatepickerToggleButton | Componente | Renderiza el botón de toggle del datepicker                             | —                                   | 1. Detectar cambios.                                                                       | Existe un `mat-datepicker-toggle` en el DOM.                                  | Garantiza que el usuario pueda abrir el calendario visualmente.             |
