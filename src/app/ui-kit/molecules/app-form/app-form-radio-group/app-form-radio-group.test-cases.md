# Test Cases — AppFormRadioGroupComponent

| ID del Test | Tipo | Escenario / Propósito | Precondiciones | Pasos clave | Resultado Esperado | Justificación de Valor |
|---|---|---|---|---|---|---|
| TC-InitialValue | Componente | Renderiza el valor inicial del `FormControl` seleccionado en el grupo | — | 1. Crear `FormControl('opcion-2')`. 2. Proveer opciones. 3. Detectar cambios. | El radio button con `value = 'opcion-2'` está seleccionado. | Garantiza la hidratación inicial desde el modelo de datos. |
| TC-FormControlSync | Componente | Actualiza el `FormControl` cuando se selecciona una opción | — | 1. Crear `FormControl(null)`. 2. Simular selección de opción. | `control.value` es la opción seleccionada. | Happy path crítico: sin esto el grupo de radios no registra la selección. |
| TC-ErrorDisplayTouched | Componente | Muestra el error cuando el control es inválido y ha sido tocado | `Validators.required` | 1. Marcar el control como tocado. 2. Detectar cambios. | Existe `mat-error` con el mensaje de error. | Garantiza el feedback de validación en el flujo estándar. |
| TC-ErrorHiddenUntouched | Componente | No muestra error cuando el control es inválido pero no tocado | `Validators.required` | 1. No tocar el control. 2. Detectar cambios. | No existe `mat-error`. | Evita errores prematuros antes de la interacción. |
| TC-CustomErrorMessage | Componente | `config.errorMessages` sobreescribe el mensaje de error por defecto | Control tocado e inválido | 1. Setear `config.errorMessages` con mensaje custom. 2. Detectar cambios. | `mat-error` muestra el mensaje personalizado. | Permite mensajes contextuales por campo. |
| TC-DisabledState | Componente | Deshabilita todas las opciones cuando el `FormControl` está deshabilitado | — | 1. Crear `FormControl({ value: null, disabled: true })`. 2. Detectar cambios. | Todos los `mat-radio-button` están deshabilitados. | Previene selección en grupos de solo lectura. |
| TC-IsRequired | Componente | `isRequired()` es `true` cuando se usa `Validators.required` | — | 1. Crear `FormControl(null, Validators.required)`. | `component.isRequired() === true`. | Garantiza la semántica correcta de campo obligatorio. |
| TC-RendersAllOptions | Componente | Renderiza todos los radio buttons para cada opción provista | 3 opciones en el array | 1. Proveer 3 opciones. 2. Detectar cambios. | Existen exactamente 3 elementos `mat-radio-button` en el DOM. | Garantiza que todas las opciones de negocio son presentadas al usuario. |
| TC-LayoutVertical | Componente | Aplica layout vertical por defecto | — | 1. No configurar `layout`. 2. Detectar cambios. | El grupo tiene las clases de layout vertical. | Garantiza el layout estándar más común. |
| TC-LayoutHorizontal | Componente | Aplica layout horizontal cuando se configura | `config.layout = 'horizontal'` | 1. Setear `config.layout = 'horizontal'`. 2. Detectar cambios. | El grupo tiene las clases de layout horizontal. | Permite adaptar el grupo a diferentes espacios en el formulario. |
| TC-IndividualOptionDisabled | Componente | Deshabilita opciones individuales marcadas como `disabled` en el array | Opción con `disabled: true` | 1. Proveer opciones con una marcada como `disabled`. 2. Detectar cambios. | Solo esa opción tiene el atributo `disabled`. | Permite deshabilitar opciones específicas sin deshabilitar todo el grupo. |
