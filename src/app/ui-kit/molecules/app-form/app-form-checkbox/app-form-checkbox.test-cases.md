# Test Cases — AppFormCheckboxComponent

| ID del Test             | Tipo       | Escenario / Propósito                                               | Precondiciones            | Pasos clave                                                                    | Resultado Esperado                               | Justificación de Valor                                                            |
|-------------------------|------------|---------------------------------------------------------------------|---------------------------|--------------------------------------------------------------------------------|--------------------------------------------------|-----------------------------------------------------------------------------------|
| TC-InitialValue         | Componente | Renderiza el valor inicial del `FormControl` en el checkbox         | —                         | 1. Crear `FormControl(true)`. 2. Setear como input. 3. Detectar cambios.       | El checkbox está marcado.                        | Garantiza la hidratación inicial correcta del estado del checkbox.                |
| TC-FormControlSync      | Componente | Actualiza el `FormControl` cuando el checkbox es activado           | —                         | 1. Crear `FormControl(false)`. 2. Simular cambio del checkbox.                 | `control.value === true`.                        | Happy path crítico: sin esta sincronización el formulario no registra el cambio.  |
| TC-ErrorDisplayTouched  | Componente | Muestra el error cuando el control es inválido y ha sido tocado     | `Validators.requiredTrue` | 1. Marcar el control como tocado. 2. Detectar cambios.                         | Existe `mat-error` con el mensaje de error.      | Garantiza el feedback de validación (ej. aceptar términos y condiciones).         |
| TC-ErrorHiddenUntouched | Componente | No muestra error cuando el control es inválido pero no tocado       | `Validators.requiredTrue` | 1. No tocar el control. 2. Detectar cambios.                                   | No existe `mat-error` en el DOM.                 | Evita errores prematuros antes de que el usuario interactúe.                      |
| TC-CustomErrorMessage   | Componente | `config.errorMessages` sobreescribe el mensaje de error por defecto | Control tocado e inválido | 1. Setear `config.errorMessages` con mensaje custom. 2. Detectar cambios.      | El `mat-error` muestra el mensaje personalizado. | Permite mensajes contextuales por campo.                                          |
| TC-DisabledState        | Componente | Deshabilita el checkbox cuando el `FormControl` está deshabilitado  | —                         | 1. Crear `FormControl({ value: false, disabled: true })`. 2. Detectar cambios. | El checkbox tiene el atributo `disabled`.        | Previene interacciones en campos de solo lectura.                                 |
| TC-IsRequiredTrue       | Componente | `isRequired()` es `true` cuando se usa `Validators.requiredTrue`    | —                         | 1. Crear `FormControl(false, Validators.requiredTrue)`. 2. Detectar cambios.   | `component.isRequired() === true`.               | Garantiza la semántica correcta de campos obligatorios como aceptar términos.     |
| TC-IsRequired           | Componente | `isRequired()` es `true` cuando se usa `Validators.required`        | —                         | 1. Crear `FormControl(false, Validators.required)`. 2. Detectar cambios.       | `component.isRequired() === true`.               | Garantiza la semántica de campo obligatorio con el validator más común.           |
| TC-MarksAsTouched       | Componente | Marca el control como `touched` al cambiar el checkbox              | —                         | 1. Simular cambio del checkbox. 2. Verificar estado del control.               | `control.touched === true`.                      | Garantiza que la validación se dispare correctamente tras la primera interacción. |
| TC-ContentProjection    | Componente | Proyecta contenido personalizado via `ng-content`                   | —                         | 1. Proveer contenido en el slot. 2. Detectar cambios.                          | El contenido proyectado aparece en el DOM.       | Permite etiquetas personalizadas con links o formato rich-text.                   |
