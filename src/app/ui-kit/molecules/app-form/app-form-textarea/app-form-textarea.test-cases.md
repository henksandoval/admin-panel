# Test Cases — AppFormTextareaComponent

| ID del Test              | Tipo       | Escenario / Propósito                                                           | Precondiciones                             | Pasos clave                                                                                       | Resultado Esperado                                                  | Justificación de Valor                                                          |
|--------------------------|------------|---------------------------------------------------------------------------------|--------------------------------------------|---------------------------------------------------------------------------------------------------|---------------------------------------------------------------------|---------------------------------------------------------------------------------|
| TC-InitialValue          | Componente | Renderiza el valor inicial del `FormControl` en el `<textarea>` nativo          | —                                          | 1. Crear `FormControl('Texto inicial')`. 2. Setear como input. 3. Detectar cambios.               | `textarea.value === 'Texto inicial'`.                               | Garantiza la hidratación inicial correcta del campo desde el modelo de datos.   |
| TC-FormControlSync       | Componente | Actualiza el `FormControl` cuando el usuario escribe en el textarea             | —                                          | 1. Crear `FormControl('')`. 2. Simular evento `input`.                                            | `control.value` refleja el texto escrito.                           | Happy path crítico: sin esto el formulario no registra los cambios.             |
| TC-ErrorDisplayTouched   | Componente | Muestra el error cuando el control es inválido y ha sido tocado                 | `Validators.required`                      | 1. Marcar el control como tocado. 2. Detectar cambios.                                            | Existe `mat-error` con el mensaje de error.                         | Garantiza el feedback de validación en el flujo estándar.                       |
| TC-ErrorHiddenUntouched  | Componente | No muestra error cuando el control es inválido pero no tocado                   | `Validators.required`                      | 1. No tocar el control. 2. Detectar cambios.                                                      | No existe `mat-error`.                                              | Evita errores prematuros antes de la interacción.                               |
| TC-CustomErrorMessage    | Componente | `config.errorMessages` sobreescribe el mensaje de error por defecto             | Control tocado e inválido                  | 1. Setear `config.errorMessages` con mensaje custom. 2. Detectar cambios.                         | `mat-error` muestra el mensaje personalizado.                       | Permite mensajes contextuales por campo.                                        |
| TC-DisabledState         | Componente | Deshabilita el `<textarea>` cuando el `FormControl` está deshabilitado          | —                                          | 1. Crear `FormControl({ value: '', disabled: true })`. 2. Detectar cambios.                       | `textarea.disabled === true`.                                       | Previene entrada en campos de solo lectura.                                     |
| TC-IsRequired            | Componente | `isRequired()` es `true` y el textarea tiene atributo `required`                | `Validators.required`                      | 1. Crear `FormControl('', Validators.required)`. 2. Detectar cambios.                             | `component.isRequired() === true` y `textarea.required === true`.   | Garantiza accesibilidad y semántica HTML correcta.                              |
| TC-ErrorOnBlurNotOnDirty | Componente | No muestra error mientras se escribe (dirty), sí lo muestra tras blur (touched) | `Validators.maxLength`                     | 1. Escribir texto excediendo el límite. 2. Verificar `errorState.shouldShow`. 3. Disparar `blur`. | En dirty: `shouldShow === false`. Tras blur: `shouldShow === true`. | Edge case de UX: el error no interrumpe al usuario mientras escribe.            |
| TC-MaxlengthError        | Componente | Muestra error cuando se supera el `maxlength` y el control es tocado            | `Validators.maxLength(10)`, control tocado | 1. Escribir más de 10 caracteres. 2. Marcar como tocado.                                          | Aparece el error de `maxlength`.                                    | Garantiza que restricciones de longitud de negocio se comuniquen correctamente. |
