# Test Cases — AppSimpleFilterComponent

| ID del Test                  | Tipo       | Escenario / Propósito                                                                            | Precondiciones                            | Pasos clave                                                                 | Resultado Esperado                                                              | Justificación de Valor                                                                        |
|------------------------------|------------|--------------------------------------------------------------------------------------------------|-------------------------------------------|-----------------------------------------------------------------------------|---------------------------------------------------------------------------------|-----------------------------------------------------------------------------------------------|
| TC-GetSelectOptionsWithReset | Componente | `getSelectOptions` antepone una opción de reset nula a las opciones provistas                    | Campo de tipo `select` con opciones       | 1. Llamar `getSelectOptions(field)`.                                        | El primer elemento del array tiene `value: null`.                               | Permite al usuario limpiar la selección de un filtro tipo select.                             |
| TC-EmitSearchNonEmpty        | Componente | `emitSearch` emite solo los criterios de campos con valor no vacío                               | Formulario con algunos campos completados | 1. Completar algunos campos y dejar otros vacíos. 2. Llamar `emitSearch()`. | `criteriaChange` emite solo los criterios de los campos con valor.              | Evita enviar al backend criterios vacíos que generan consultas incorrectas.                   |
| TC-DefaultOperatorByType     | Componente | `emitSearch` usa el operador por defecto según el tipo del campo                                 | Campo sin `defaultOperator` explícito     | 1. Completar un campo de tipo `text`. 2. Llamar `emitSearch()`.             | El criterio emitido usa el operador default para `text`.                        | Garantiza que los filtros tienen un operador válido incluso sin configuración explícita.      |
| TC-CustomDefaultOperator     | Componente | `emitSearch` usa `field.defaultOperator` cuando está configurado                                 | Campo con `defaultOperator` explícito     | 1. Completar ese campo. 2. Llamar `emitSearch()`.                           | El criterio emitido usa el `defaultOperator` del campo, no el default del tipo. | Permite personalizar el operador de filtrado por campo específico.                            |
| TC-ClearAllCriteria          | Componente | `clearAllCriteria` resetea todos los controles y emite criterios vacíos                          | Formulario con valores completados        | 1. Llamar `clearAllCriteria()`.                                             | Todos los controles quedan en su valor vacío. `criteriaChange` emite `[]`.      | Garantiza el reset completo del estado de filtrado.                                           |
| TC-ToggleChangeIncluded      | Componente | `onToggleChange` actualiza `currentToggles` e incluye los criterios de toggle en el próximo emit | Toggle configurado en el footer           | 1. Cambiar el estado de un toggle. 2. Llamar `emitSearch()`.                | El toggle activo se incluye en los criterios emitidos.                          | Garantiza que los filtros de toggle de búsqueda rápida funcionen junto con los de formulario. |
