# Test Cases — AppAdvancedFilterComponent

| ID del Test                     | Tipo       | Escenario / Propósito                                                                   | Precondiciones                                         | Pasos clave                                                                    | Resultado Esperado                                                                             | Justificación de Valor                                                              |
|---------------------------------|------------|-----------------------------------------------------------------------------------------|--------------------------------------------------------|--------------------------------------------------------------------------------|------------------------------------------------------------------------------------------------|-------------------------------------------------------------------------------------|
| TC-OperatorOptions              | Componente | `operatorOptions` retorna solo los operadores aplicables al tipo del campo seleccionado | Config con campos de distintos tipos                   | 1. Seleccionar un campo de tipo `text`. 2. Leer `operatorOptions()`.           | Solo los operadores para `text` están disponibles (ej. no aparece "mayor que").                | Evita combinaciones de campo/operador inválidas que rompan la consulta del backend. |
| TC-CanAddCriterionNoSelection   | Componente | `canAddCriterion` es `false` cuando no hay campo u operador seleccionado                | —                                                      | 1. No seleccionar campo ni operador. 2. Leer `canAddCriterion()`.              | Retorna `false`.                                                                               | Previene criterios incompletos que generarían filtros inválidos.                    |
| TC-CanAddCriterionRequiresValue | Componente | `canAddCriterion` es `false` cuando el operador requiere valor pero el valor está vacío | Operador con `requiresValue: true`                     | 1. Seleccionar campo y operador que requiere valor. 2. Dejar el valor vacío.   | Retorna `false`.                                                                               | Edge case: criterios sin valor causan errores en la consulta del backend.           |
| TC-CanAddCriterionWithValue     | Componente | `canAddCriterion` es `true` cuando el operador requiere valor y el valor está definido  | —                                                      | 1. Seleccionar campo, operador y escribir valor.                               | Retorna `true`.                                                                                | Happy path: habilita el botón de agregar cuando el criterio es válido.              |
| TC-CanAddCriterionNoValueNeeded | Componente | `canAddCriterion` es `true` cuando el operador no requiere valor                        | Operador con `requiresValue: false` (ej. "está vacío") | 1. Seleccionar campo y operador que no requiere valor.                         | Retorna `true` aunque el valor esté vacío.                                                     | Garantiza que operadores unarios funcionen correctamente.                           |
| TC-AddCriterion                 | Componente | `addCriterion` agrega el criterio, resetea el formulario y emite `criteriaChange`       | Formulario válido completado                           | 1. Completar selección de campo, operador y valor. 2. Llamar `addCriterion()`. | La lista de criterios crece en 1, el formulario se resetea y `criteriaChange` emite.           | Happy path crítico del flujo principal de filtrado avanzado.                        |
| TC-MaxCriteriaLimit             | Componente | `addCriterion` no agrega más criterios cuando se alcanza `maxCriteria`                  | `config.maxCriteria = 3`, ya hay 3 criterios           | 1. Intentar agregar un criterio más.                                           | La lista no crece y no emite.                                                                  | Evita sobrecargar el backend con demasiados criterios de filtrado.                  |
| TC-RemoveCriterion              | Componente | `removeCriterion` elimina solo el criterio con el id correspondiente y emite            | Múltiples criterios activos                            | 1. Llamar `removeCriterion(id)` con el id de un criterio específico.           | Solo ese criterio desaparece. El resto permanece. `criteriaChange` emite el array actualizado. | Garantiza que eliminar un filtro no afecte a los demás filtros activos.             |
| TC-ClearAllCriteria             | Componente | `clearAllCriteria` vacía todos los criterios y emite array vacío                        | Criterios activos sin toggles activos                  | 1. Llamar `clearAllCriteria()`.                                                | `criteriaChange` emite `[]`.                                                                   | Garantiza el reset completo del estado de filtrado.                                 |
